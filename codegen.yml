overwrite: true
schema: "./schema.graphql"
documents: "**/*.graphql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-document-nodes"
      - "typescript-urql"
    config:
      withHooks: true
